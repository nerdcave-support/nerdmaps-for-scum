# nerdmaps-for-scum
A self-hosted web-based live map tool for SCUM dedicated servers. Displays player positions, shared map markers, location pings, sector grid overlay, and per-player follow/hide controls. Built with PHP, SQLite, and vanilla JS. Requires access to the SCUM server's SCUM.db file. Player positions update approximately once per minute.
